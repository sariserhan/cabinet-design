{
  "name": "catalog-compiler",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "tsx --test tests/core/*.test.ts",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src tests/core",
    "check:benchmark": "python3 tools/check_benchmark_packet.py",
    "check": "npm run lint && npm run typecheck && npm test && npm run check:benchmark"
  },
  "dependencies": {
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/node": "^26.5.1",
    "eslint": "^10.10.0",
    "tsx": "^4.23.13",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.70.0"
  }
}
