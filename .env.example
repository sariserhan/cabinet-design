# Populated by `npx convex dev` and Convex Auth setup.
CONVEX_DEPLOYMENT=
NEXT_PUBLIC_CONVEX_URL=
NEXT_PUBLIC_CONVEX_SITE_URL=
# Generate a private random value; configure the same value on the Convex deployment.
CATALOG_WORKER_SECRET=
# Configure at least one locally. Never commit actual API keys.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
# Optional provider:model -> actual USD prices per million tokens. Unknown cost stays unavailable.
CATALOG_MODEL_PRICES_JSON={}
